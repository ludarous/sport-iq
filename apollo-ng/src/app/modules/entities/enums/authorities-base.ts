import {EnumWrapper} from '../enum-wrapper';

export class AuthoritiesBase extends EnumWrapper {
  constructor(enumObject: any, value?: number | string, data?: any) {
    super(enumObject, value);
  }
}
